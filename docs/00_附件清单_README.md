# 附件清单 —— LKUS 短信攻击分析任务包

配合 `claude_code_prompt_LKUS_SMS_attack.md`（任务书，建议改名为 `TASK.md`）一起放进 Claude Code 的工作目录。

## 建议目录结构

```
项目根/
├── TASK.md                      ← 任务书（原 claude_code_prompt_LKUS_SMS_attack.md）
├── docs/
│   ├── 00_附件清单_README.md      ← 本文件
│   ├── 01_090926短信分析_strategy_ARqkLD7E3JaK.md
│   ├── 02_风控策略运营SOP-执行版0729.md
│   ├── 03_090426短信黑产攻击行为初步分析与结论.md
│   ├── 04_last24hr_数据字典与基线.md
│   └── 05_监控与运营证据_数值转录.md
├── data/
│   └── last24hr.xlsx            ← 103 MB，原始上传文件，需自行放入
├── evidence/                    ← 原始截图（可选，用于人工复核）
└── out/                         ← Claude Code 产出目录
```

## 各文件用途

| 文件 | 在任务里的角色 | Claude Code 该怎么用 |
|---|---|---|
| `01_..._strategy_ARqkLD7E3JaK.md` | **待评估策略的 6 小时评估原文** | 复现它的九项口径；重算并解释文末指出的第 3/4 项标签互换与第 9 项跨窗口污染。这是 R1 和 R3 的起点。 |
| `02_风控策略运营SOP...md` | **上线门槛与黑产评估的制度依据** | 发布前评估分级标准表（§二）直接用作 R1 的 gate table；黑产评估标准（§三）直接用作 R5 的分类框架与 §3.7 不得单一特征定性原则。 |
| `03_090426短信黑产...md` | **120 天历史基线 + 已有 SQL** | 拿它的 SQL 当取数模板（注意需按 Phase 0 的实际 schema 修正）；拿它的国家对分布当长周期基线，验证「9 月攻击是 8 月趋势的延续」。文末 +92 号码的 IP 来源分布是「攻击方已在用美国/德国 IP」的历史佐证。 |
| `04_last24hr_数据字典与基线.md` | **对账基准 + 词汇表** | Phase 2 数据完整性检查的比对对象；九项指标的 24h 重算值可直接对账；§8 策略清单与 §9 特征/规则全集是写新策略必须遵守的词汇。 |
| `05_监控与运营证据_数值转录.md` | **数据库里查不到的数字** | Grafana 总量用于验证取数完整性；upush 日发送量是 R6 压制曲线的主坐标系；§6 的新加坡案例是 R5 必须模仿的行文格式。 |
| `data/last24hr.xlsx` | **离线数据集** | 103 MB，**不要整表打印**。用 pandas / `openpyxl(read_only=True)` 读。`策略命中` 表含 `uid`/`userAgent`/`ruleDetail` 等主表没有的字段，是设备维度分析的唯一现成来源。 |

## 关于 PDF 原件

三份 Feishu 文档的原始 PDF 是**纯截图（无文字层）**，`pdftotext` 取不到任何内容。上面的 `.md` 是 OCR + 人工核对的结果，数字已逐个对照原图校验。**如需引用原文措辞或补齐截断部分，请回 Feishu 原文。**

已知截断：

- `01`：截图止于「六、综合分析」，**七、最终上线结论**（目录显示为 🟡 暂不建议直接全量上线）与**八、上线注意事项 / 9.1 Token 空值不要视为低分**未被覆盖。
- `02`：截图止于「四、执行模板一」表格中部，执行模板二及之后未覆盖。
- `03`：截图止于 +92 国家码分布列表尾部。

## 还缺什么（Phase 0 需要自行获取或找人要）

| 缺失项 | 影响 | 找谁 |
|---|---|---|
| **数据库连接配置** | 一切分析的前提 | 曾翔宇（大数据平台） |
| **RMS 策略全表导出**（id ↔ 名称 ↔ 状态 ↔ 上线时间 ↔ 规则 id） | 没有上线时间就画不出 R6 的措施时间轴，压制效果无法归因到具体策略 | 田志鲔 |
| **upush / Twilio 发送明细**（含 country、status、price） | 成本估算、REJECT 是否真的阻止了发送 | upush 侧 |
| **OTP 校验事件**（用户提交验证码的场景） | 「发了短信但从未校验」是判定 pumping 最硬的标签，目前完全缺失 | 待确认落在哪个 scene / 表 |
| **`strategy_uKSgJAVWhWlU` 的 Feishu 评估文档** | R1b 上线后观察需要对照它的上线前评估 | David 本人（链接 `CX0fwn80yimM4UkXLo4cQU5onMh`） |
| **账号/订单表**（可按 `user_no`/`phone_no` 关联） | 正常用户基线与误伤估算 | 待确认 |

## 处理提醒

- 报告里手机号需脱敏（保留区号 + 前 3 位），IP 不写全段，除非是黑产评估依据里必须的样本表。
- `04` 与 `05` 中的数字全部来自 24 小时快照或大盘截图，**不能当成长期结论**；正式报告的每个数字都要从数据库重算一遍。
- 涉及 9/8–9/9 的窗口，先证明数据完整再出数（见 `04` §4 的逐小时表与 09-08T19–T21 塌陷说明）。
