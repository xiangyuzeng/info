# `out3/docs/` 与 `out3/redacted/` 的来源说明

**结论先写**：这 20 个文件**不是本轮分析管道（`sms_attack/r3_*`）的产出**。在人工确认之前，**不要推送到公开仓**。

---

## 一、事实

`out3/` 下出现两个目录，非本次会话生成：

| 目录 | 文件数 | 内容 |
|---|---|---|
| `out3/docs/` | 10 | `2026-09-12_north-america-push-{ARqkLD7E3JaK, rDf6oPcZ8ydk, s1…s9}-strategy-approval.md` |
| `out3/redacted/` | 10 | 同名 `_public.md` 版本 |

它们使用了与本轮分析相同的评估窗口（`2026-09-11 23:38:34 ~ 2026-09-12 23:38:34 UTC`）与相同的候选编号（S1/S2/S3/S4/S6/S7/S8/S9），说明是基于同一份数据、由并行的另一次会话产出。
同一时间 `MEMORY.md` 也新增了两条本会话未写入的记录（`risk-strategy-approval-doc-format`、`round3-supersedes-round2-candidates`，后者写着「动手前先看 `/app/out3/` 有无现成产物」）。

**因此判断为并行会话的正当工作产物，不是异常写入。** 本轮未删除、未修改它们。

## 二、与本轮产出的差异

### 2.1 格式：与 `docs/06` 模板不一致

| 项 | `docs/06` 模板 / 本轮产出 | `out3/docs/` |
|---|---|---|
| 一级小节 | `**一、配置前评估表（建议建为多维表格）**`（整串加粗的普通段落） | `## 一、配置前评估表（建议建为多维表格）`（H2 标题） |
| 二级小节 | `二、**发布前评估表（建议建为多维表格）**`（`二、`在粗体外） | `## 二、发布前评估表（建议建为多维表格）` |
| 表头单元格 | `\|**字段名**\|**字段类型**\|…` | `\| 字段名 \| 字段类型 \|…`（无加粗、带空格） |
| 额外小节 | 无（任务书明确要求「不得增加小节」） | 多出 `## 数据来源与口径说明` |
| 文档标题 | `20260912_审批_北美push场景策略上线审批_<strategy_id>` | `20260912_审批_北美push场景策略审批_S9` |

字段行数两边都是 64 行（23 + 41），**字段名/类型/必填三列内容一致**；差异集中在标题层级与表格样式。
按任务书 §9「两个小节标题、四列表、行序与字段类型必须逐行一致，不得增删小节」，本轮产出（`out3/审批_*.md`）已逐行 diff 校验通过。

### 2.2 内容：包含本轮已否决的候选

`out3/docs/` 为 S1–S9 每条候选都出了一份审批表，其中至少三条在本轮 `03_候选特征策略与回测.md` 里已明确否决：

| 候选 | 本轮否决理由 |
|---|---|
| S9 `网关uid 黑名单` | 热点 uid **次日留存仅 0–5%**，名单配置完第二天即失效；84.4% 覆盖率是 fit-on-self 的假象 |
| S6 / S7 `ip C段黑名单` | 日留存中位仅 ~43%，建议作为每日刷新的运营动作，不做静态策略 |
| S8 `version ≤1.4.30` | **100% 被「token 缺失或空」包含**，不增加任何覆盖，只多一个会过期的维度 |

这三条的留存率/包含关系是本轮新测的（见 `01_` 第四节、`03_` 第三节），并行会话的产出时点可能早于该结论。

## 三、建议处理

1. **推送公开仓时只推本轮产出**：`out3/*.md`（00–07 报告 + 5 份 `审批_*.md`）。
2. `out3/docs/` 与 `out3/redacted/` 由 David 确认来源与用途后再决定去留；若确认作废，建议删除以免与本轮审批表并存造成审批混淆。
3. 无论如何 `out3/data/` 不进 git（已在 `.gitignore` 中）。

---

## 四、已按 David 决定「全部推送」—— 推送前的逐项核对结果

已对 `out3/docs/`（10 份，含完整规则原文）与 `out3/redacted/`（10 份 `_public`，规则原文已打码）全部 20 个文件做安全扫描与数值核对。

### 4.1 安全扫描：通过
无完整 IPv4、无内网地址（10./192.168./172.16-31.）、无手机号长数字串、无任何凭据或 token 明文。

⚠️ 注意二者关系：`redacted/` 是把规则原文替换成 `〔已脱敏：规则原文〕` 的公开版。**同时推送 `docs/` 会使 `redacted/` 的打码失去意义**。鉴于本轮产出的 5 份审批表本身即包含完整引擎规则原文（已按「全文照发」决定），`docs/` 不构成额外暴露，故一并推送；`redacted/` 保留但实际冗余。

### 4.2 值得肯定的一致之处
两套文档在**准确率口径**上独立得出同一结论：并列给出「读法A =(命中−风险−暂无法判断)/命中」与「读法B =(命中−风险)/命中」，并明确**在田志鲔确认前不下定论**。与本轮 `05_` B1 完全一致。

### 4.3 🔴 与本轮结论的实质性冲突（审阅时必须一并看）

| 文件 | 其结论 | 本轮结论 |
|---|---|---|
| `s9-uid-blacklist` | 命中 4,079 pv、误伤 0、样本量达标 | **该策略不可上线**：热点 uid **次日留存仅 0–5%**（`01_` 第四节逐日实测），名单配置完次日即失效；4,079 的覆盖是 fit-on-self 的假象。该文件**未提及留存率问题** |
| `s6/s7-ipc-blacklist` | 命中 1,804 pv、误伤 0 | 覆盖率属实，但 /24 名单日留存中位仅 ~43%，**只能作为每日刷新的运营动作**，不宜做静态策略。该文件未提及 |
| `s8-cid105-old-version` | 独立候选，命中 699 pv | 版本条件 **100% 被「token 缺失或空」包含**，不增加任何覆盖。另：规则原文写作 `version 版本号小于 1.4.30`，而引擎实有的是 `version 版本号小于等于 1.4.30`（`rule_gRgP1npFHg1e`，VERSION_LESS_THAN），**该措辞在 RMS 里没有对应 rule** |
| `s2-multi-cid-no-token` | 误伤 0.7021%（7/997） | 与本轮一致：cid 放宽到 106/108 会显著增加误伤，故本轮 R3C1 收窄到 cid=105 |

（`s6` 与 `s7` 数值完全相同**不是错误**：境外流量几乎全部为 cid=105，加不加该条件命中集一致，本轮回测同样得到两者相等。）

### 4.4 窗口口径不统一
`s1`–`s9` 九份统一用最近 24h（整体调用量 5,554，与本轮 24h 全区号 5,555 吻合）；
但 `ARqkLD7E3JaK` 用 8,384、`rDf6oPcZ8ydk` 用 57,256，二者窗口彼此不同、也与 s 系列不同。
本轮对这两条策略采用**自各自上线时刻起**的窗口（13,074 / 13,068），因为表二问的是「上线后观察」。
**两套数字都不算错，但分母不同，不可直接相互比较。**

### 4.5 格式
20 个文件的小节标题层级与表格样式与 `docs/06` 模板不一致（详见第二节），本轮 5 份审批表已逐行 diff 校验通过。若要提交飞书审批，建议以本轮 `out3/审批_*.md` 为准。
