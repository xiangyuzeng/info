# 群回复草稿（发送前请 David 过一遍）

---

## 一、回段枝宏

> 按您说的流程跑了一轮：先拉最近 24 小时放行数据，再从里面看特征，然后出策略。
>
> **数据**：最近 24h 非美国区号放行 1,504 条，其中非美国 IP 1,247 条。取的是风控源库 64 张分片表，不走数仓。
>
> **三条特征结论**：
> 1、**低分已经拦干净了**——放行数据里 reCAPTCHA 分数 ≤0.3 的是 **0 条**。172、策略3、策略4 把低分人群吃尽了，再加分数类策略新增召回为 0。
> 2、**您提的 token 缺失已经发生**——放行里 41.9%（523/1247）不带 token 或 token 为空串。
> 3、**剩下 58.1% 是高分养号**——带 token 且分数 0.5~0.9。这部分现有分数策略同样无效。
>
> **召回**：按 SOP 两类证据口径，最近 24h 非 +1/+86 判黑 3,959 条，已拦 2,479 条，**召回 62.6%**（第一轮同口径 53.8%）。
> upush 侧境外日发送从 09-09 峰值 2,337 降到 09-11 的 1,175，**策略3/4 降了约 43%，且 +1 用户填充率全程 94%~97% 没受影响**。
> 但离 150 条/天的目标还差 8 倍，**召回不 OK**。
>
> **出两条特征策略，申请预上线观察**：
> - **R3C1**：非+1/+86 && cid=105 && (token 不存在 || token 为空串)。覆盖放行的 41.5%。
> - **R3C2**：非+1/+86 && cid=105 && IP 来源 ∈ 德国,阿联酋,巴西,菲律宾,加拿大,瑞典,爱沙尼亚,智利（这 8 国 35 天内 OTP 填充数为 0）。覆盖 27.3%。
>
> 两条都不含任何区号/IP 级计数器，不会出现「黑产打满阈值后正常用户也进不来」。两条合起来召回可到 **83.8%**，境外日发送预计降到 ~559 条。
>
> **有两件事要您定**：
> 1、R3C1 有 4 条误伤样本，都是**老版本安卓客户端本来就不发 token 的真实境外用户**（+65 新加坡 1.4.16、+65 新加坡 1.3.65、+60 罗马尼亚 1.4.42、+852 香港 1.3.32），全攻击期误伤率 0.115%，略高于 SOP 的 0.1%。加 `version ≥ 1.4.30` 护栏能把误伤降到 0，但覆盖率从 41.5% 掉到 7.1%。**这个取舍请您拍。**
> 2、**即使两条都上，境外日发送预计仍有 ~559 条，是目标的 3.7 倍。** 特征策略这一轮能拿到的空间就这么多，剩下的建议走 upush/Twilio 侧对零基线目的国做地区权限或日上限。
>
> 另外两个发现供参考：
> - 预上线里已有一条 `strategy_bTCEWBZggaAP` 用了 token 缺失条件，方向对，但它前面挂了「同区号近60分钟未携带token访问次数>5」这个**区号级计数器**，属于您否掉的那一类；而且 `version ≥1.4.30` 把本轮 token 缺失漏出的 82%（1.3.40/1.3.50/1.4.26）排除在外。所以本轮另出策略，没复用它。
> - `+220`（冈比亚）已经是第一大目标区号，而第一轮名单里的 `+504` 基本消失了。**目标区号在轮换**，这也是不建议用区号名单当主策略的实证理由。
>
> 审批表（配置前 + 发布前两张表）已按 SOP 模板填好，172、策略3、策略4 的也补了回溯版。**审批人、审批结论、上线后监控责任人几格留空等您。**
> 还有一件要请田志鲔确认：`风险样本量` 和 `暂无法判断样本量` 的定义、以及准确率公式。按「(命中−风险−暂无法判断)÷命中」算，三条在线策略准确率是 92%~94%，卡在 SOP 的 99.9% 下面；按只扣风险样本量算就是 100%。**三条策略的误伤都是 0**，差别全在「暂无法判断」怎么算。

---

## 二、回 David

> 翔宇这边跑完了，数据和表都在 `out3/`，几个要你决定的点：
>
> **1、候选只留了两条，其他都否了，理由写在 `03_候选特征策略与回测.md`：**
> - `cid=105` 单条覆盖 98.7%，但 OTP 已填充的真实用户里有 **70 条**也是 cid=105 —— 不能单独上。
> - `网关uid 黑名单` 回测覆盖 84.4% 很漂亮，但热点 uid **次日留存只有 0~5%**，配完第二天就失效，纯粹是 fit-on-self。否了。
> - `ip C段黑名单` 覆盖 64%、误伤 0，但日留存中位只有 ~43%。**建议当成每天刷新的运营动作，不要做成静态策略。**
> - `version ≤1.4.30` 覆盖 34.5%，但**100% 被 token 缺失包含**，不增加任何覆盖，只多一个会过期的维度。
>
> **2、要你人工看的（`05_人工确认清单.md` B2）：**
> - `out3/data/pass_24h_setA.csv` 随机开 20 行，看画像对不对得上。
> - R3C1 那 4 条误伤样本逐条开一下，确认是不是真实用户 —— 这 4 条就是审批表里的「风险样本量」。
> - R3C2 名单里 **加拿大/瑞典/爱沙尼亚/智利** 单国漏出只有 19~21 条，证据薄，你看要不要先去掉，只留德国/阿联酋/巴西/菲律宾。
>
> **3、要问田志鲔的三件事：**
> - `cid 包含 105`（单独 105）现在没有现成 rule，只有 `106,105` / `106,105,108` / `106,105,108,200`。新建一条同类型的算不算纯配置、要不要排期？
> - `风险样本量` / `暂无法判断样本量` 定义 + 准确率公式（直接决定三条在线策略算不算达标）。
> - **极验下掉之后 REVIEW 现在到底做什么**？弹验证码、还是只记录、还是直接拒？现在所有候选都只能写「拦截」，因为不敢假设 REVIEW 还有挑战能力。
>
> **4、一个时序问题**：RMS 里策略3/策略4 的 `update_time` 是 **09-10 15:50/15:51 UTC**，比段总 09-11 的书面批准早一天。审批表里「预上线开始时间/实际观察时长」我按 RMS 记录填了，你确认下这个时序对不对，或者 update_time 是不是改了别的字段。
>
> **5、推送前**：`out3/data/` 里是**不脱敏**的手机号和完整 IP，已经加进 `.gitignore` 不会进 git。但报告正文写了「当前哪些流量还能漏过去」和「马上要上哪些规则」，推到 public 仓之前你先过一遍口径。
